(function(){
  const D=window.NYC_DATA, S=window.TripStore;
  const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>Array.from(r.querySelectorAll(s));
  const page=document.body.dataset.page||'home';
  const fmtTime=t=>t||'TBD';
  const typeIcon=t=>({drive:'🚗',food:'🍴',place:'📍'}[t]||'📍');
  const travelIcon=t=>({walk:'🚶',transit:'🚇',drive:'🚗',ferry:'⛴️'}[t]||'➡️');
  const mapUrl=q=>`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(q)}`;
  const escapeHtml=(s='')=>String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const minutesLabel=ts=>{if(!ts)return'';const m=Math.round((Date.now()-ts)/60000);if(m<1)return'Updated just now';if(m<60)return`Updated ${m} min ago`;const h=Math.round(m/60);return`Updated ${h} hr${h===1?'':'s'} ago`;};

  function nav(){
    const mobile=[['index.html','home','⌂','Home'],['itinerary.html','itinerary','◷','Itinerary'],['food.html','food','♨','Food'],['drive.html','drive','➜','Drive'],['more.html','more','•••','More']];
    const desktop=[['index.html','home','Home'],['itinerary.html','itinerary','Itinerary'],['food.html','food','Food'],['drive.html','drive','Drive'],['weather.html','weather','Weather'],['links.html','links','Links'],['lists.html','lists','Lists']];
    document.body.insertAdjacentHTML('afterbegin',`<header class="site-header"><a class="brand" href="index.html" aria-label="NYC 2026 home"><span class="brand-mark">NYC</span><span><b>NYC 2026</b><small>Toronto → New York City</small></span></a><nav class="desktop-nav">${desktop.map(([href,id,label])=>`<a href="${href}" class="${page===id?'active':''}">${label}</a>`).join('')}</nav></header>`);
    document.body.insertAdjacentHTML('beforeend',`<nav class="bottom-nav" aria-label="Main navigation">${mobile.map(([href,id,icon,label])=>`<a href="${href}" class="${page===id?'active':''}"><span>${icon}</span><small>${label}</small></a>`).join('')}</nav>`);
  }
  function setTopTheme(){const m=$('meta[name="theme-color"]');if(m)m.content=matchMedia('(prefers-color-scheme: dark)').matches?'#0b1220':'#f4efe6';}
  function isoDateInTripTZ(date=new Date()){const p=new Intl.DateTimeFormat('en-US',{timeZone:D.trip.timezone,year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(date);const o=Object.fromEntries(p.filter(x=>x.type!=='literal').map(x=>[x.type,x.value]));return`${o.year}-${o.month}-${o.day}`;}
  function tripDayForNow(){const today=isoDateInTripTZ();if(D.days.some(d=>d.key===today))return today;if(today<D.trip.startDate)return D.trip.startDate;return D.trip.returnArrivalDate;}
  function countdownText(){const now=new Date(),start=new Date('2026-09-12T09:00:00-04:00'),end=new Date('2026-09-16T02:00:00-04:00');if(now<start){const diff=Math.ceil((start-now)/86400000);return diff<=1?'Tomorrow: NYC begins':`${diff} days until NYC`;}if(now<=end){const today=isoDateInTripTZ(now),idx=D.days.findIndex(d=>d.key===today);if(today==='2026-09-16')return'Heading home · final stretch';return idx>=0?`Day ${Math.min(idx+1,4)} of 4 · ${D.days[idx].label}`:'NYC 2026';}return'NYC 2026 · trip complete';}
  function allStopsForDay(date){return[...(D.itinerary[date]||[]).map(x=>({...x,custom:false})),...S.customStops().filter(x=>x.day===date).map(x=>({...x,custom:true}))];}
  function progress(date){const stops=allStopsForDay(date).filter(x=>!x.routeMarker),done=stops.filter(x=>S.status(x.id)==='done').length;return{done,total:stops.length,pct:stops.length?Math.round(done/stops.length*100):0};}
  function nextStop(date){return allStopsForDay(date).find(x=>!x.routeMarker&&S.status(x.id)!=='done')||null;}
  function weatherMini(w){return w?`<span class="weather-mini">${w.icon} ${w.temp}° · Rain ${w.rain??'—'}%</span>`:`<span class="weather-mini muted">Forecast closer to trip</span>`;}
  function ticketBadge(s){if(s==='required')return'<span class="badge badge-ticket">🎟 Ticket</span>';if(s==='booked')return'<span class="badge badge-booked">✓ Booked</span>';return'';}
  function tripTime24(){const p=new Intl.DateTimeFormat('en-US',{timeZone:D.trip.timezone,hour:'2-digit',minute:'2-digit',hourCycle:'h23'}).formatToParts(new Date()),o=Object.fromEntries(p.filter(x=>x.type!=='literal').map(x=>[x.type,x.value]));return`${o.hour}:${o.minute}`;}
  function humanTime(t){if(!/^\d{2}:\d{2}$/.test(t||''))return t||'';const[h,m]=t.split(':').map(Number);return new Intl.DateTimeFormat('en-US',{hour:'numeric',minute:'2-digit'}).format(new Date(2000,0,1,h,m));}
  function statusButton(item){const r=S.statusRecord(item.id),done=r.status==='done';return`<div class="status-wrap"><button class="status-toggle ${done?'done':''}" data-status-id="${item.id}" aria-pressed="${done}">${done?'✓ Done':'○ Pending'}</button>${done?`<button class="done-time" data-edit-time="${item.id}" title="Edit completion time">${r.completedTime?`Done ${humanTime(r.completedTime)} · edit`:'Add done time'}</button>`:''}</div>`;}
  function selectedRestaurantName(mealId){const sel=S.selectedRestaurant(mealId),r=[...D.curatedRestaurants,...S.customRestaurants()].find(x=>x.id===sel);return r?.name||null;}
  function transitClass(line){const x=String(line||'').toUpperCase();if(['1','2','3'].includes(x))return'red';if(['4','5','6'].includes(x))return'green';if(['7'].includes(x))return'purple';if(['A','C','E'].includes(x))return'blue';if(['B','D','F','M'].includes(x))return'orange';if(['N','Q','R','W'].includes(x))return'yellow';if(x==='S')return'gray';return'bus';}
  function transitConnector(item){
    if(!item.travelToNext||item.travelToNext==='—')return'';
    const p=item.transitPlan;
    if(!p)return`<div class="travel-connector"><span>${travelIcon(item.travelMode)} ${escapeHtml(item.travelToNext)} to next</span></div>`;
    const badges=(p.lines||[]).map(x=>`<span class="route-badge ${transitClass(x)}">${escapeHtml(x)}</span>`).join('');
    const steps=(p.steps||[]).map((x,i)=>`<li><b>${i+1}</b><span>${escapeHtml(x)}</span></li>`).join('');
    return`<div class="travel-connector transit-detailed"><details class="transit-card"><summary><span class="transit-summary-icon">${p.mode==='bus'?'🚌':'🚇'}</span><span class="transit-summary-copy"><b>${escapeHtml(item.travelToNext)} to next</b><small>${escapeHtml(p.summary||'Transit directions')}</small></span><span class="route-badges">${badges}</span><span class="transit-chevron">⌄</span></summary><div class="transit-body"><div class="transit-direction"><span>${p.mode==='bus'?'Bus direction':'Train direction'}</span><b>${escapeHtml(p.direction||'')}</b></div><ol class="transit-steps">${steps}</ol>${p.note?`<p class="transit-note">⚠ ${escapeHtml(p.note)}</p>`:''}<div class="transit-actions"><a class="btn btn-primary" href="${p.liveUrl}" target="_blank" rel="noopener">Open Live Transit</a><a class="btn btn-secondary" href="https://www.mta.info/planned-service-changes" target="_blank" rel="noopener">MTA Service Changes</a></div></div></details></div>`;
  }

  function listBlock(title,arr){return arr?.length?`<section class="detail-block"><h4>${title}</h4><ul>${arr.map(x=>`<li>${escapeHtml(x)}</li>`).join('')}</ul></section>`:'';}
  function detailRows(item){
    const d=item.details||{};let html='';
    if(d.about)html+=`<section class="detail-block"><h4>About</h4><p>${escapeHtml(d.about)}</p></section>`;
    if(d.history)html+=`<section class="detail-block"><h4>History</h4><p>${escapeHtml(d.history)}</p></section>`;
    html+=listBlock('What to see',d.whatToSee)+listBlock('Route / plan',d.routeSteps)+listBlock('Interesting facts',d.funFacts)+listBlock('Must know',d.mustKnow)+listBlock('Visit tips',d.tips)+listBlock('Recommendations',d.recommendations);
    if(d.schedule?.length)html+=`<section class="detail-block"><h4>Exact visit schedule</h4><div class="mini-schedule">${d.schedule.map(x=>`<div class="schedule-row"><time>${escapeHtml(x.time)}</time><div><b>${escapeHtml(x.title)}</b>${x.note?`<small>${escapeHtml(x.note)}</small>`:''}</div></div>`).join('')}</div></section>`;
    if(d.floorGuide?.length)html+=`<section class="detail-block"><h4>Floor guide</h4><div class="floor-guide">${d.floorGuide.map(x=>`<div><span>Floor ${escapeHtml(x.floor)}</span><b>${escapeHtml(x.title)}</b><small>${escapeHtml(x.note||'')}</small></div>`).join('')}</div></section>`;
    if(d.bollywood?.length)html+=`<section class="detail-block bollywood-block"><h4>🎬 Bollywood connection</h4><div class="film-list">${d.bollywood.map(x=>`<a href="${x.url}" target="_blank" rel="noopener"><b>${escapeHtml(x.title)} ↗</b><small>${escapeHtml(x.note||'')}</small></a>`).join('')}</div></section>`;
    const links=item.links||{}, labels={official:'Official website',history:'History / learn more',map:'Map',app:'App / digital guide',menu:'Menu',halls:'All halls',starry:'The Starry Night',tickets:'Tickets'};
    const linkBtns=Object.entries(links).filter(([,u])=>u).map(([k,u])=>`<a class="chip-link" href="${u}" target="_blank" rel="noopener">${labels[k]||escapeHtml(k)} ↗</a>`);
    (item.extraLinks||[]).forEach(x=>linkBtns.push(`<a class="chip-link" href="${x.url}" target="_blank" rel="noopener">${escapeHtml(x.label)} ↗</a>`));
    if(item.mapQuery)linkBtns.push(`<a class="chip-link" href="${mapUrl(item.mapQuery)}" target="_blank" rel="noopener">Directions ↗</a>`);
    if(linkBtns.length)html+=`<section class="detail-block"><h4>Useful links</h4><div class="chip-row">${linkBtns.join('')}</div></section>`;
    if(item.mealSlot){const name=selectedRestaurantName(item.mealSlot);html+=`<section class="detail-block"><h4>Restaurant</h4><p>${name?`<b>${escapeHtml(name)}</b> is selected for this meal.`:'No restaurant selected yet.'}</p><a class="text-link" href="food.html#${item.mealSlot}">View meal options →</a></section>`;}
    html+=`<section class="detail-block"><h4>Notes</h4><textarea class="note-field" data-note-id="${item.id}" placeholder="Add your note…">${escapeHtml(S.note(item.id))}</textarea><small class="save-note">Saved locally on this device.</small></section>`;
    return html;
  }
  function renderTimelineCard(item,weatherData,date){const hourly=/^\d{2}:\d{2}$/.test(item.time||'')?TripWeather.hourlyFor(weatherData,date,item.time):null,selected=item.mealSlot?selectedRestaurantName(item.mealSlot):null,subtitle=selected?`<b>${escapeHtml(selected)}</b> · ${escapeHtml(item.subtitle||'')}`:escapeHtml(item.subtitle||''),klass=`${item.routeMarker?'route-marker':''} ${S.status(item.id)==='done'?'is-done':''}`;return`<article class="timeline-item ${klass}" id="${item.id}"><div class="timeline-time">${fmtTime(item.time)}</div><div class="timeline-line"><span class="timeline-dot">${typeIcon(item.type)}</span></div><div class="trip-card"><div class="card-topline"><div class="card-badges">${ticketBadge(item.ticketStatus)}${item.custom?'<span class="badge">Added by you</span>':''}</div>${!item.routeMarker?statusButton(item):'<span class="badge badge-route">Route marker</span>'}</div><h3>${escapeHtml(item.title)}</h3>${subtitle?`<p class="card-subtitle">${subtitle}</p>`:''}<div class="meta-row">${item.duration&&item.duration!=='—'?`<span>⏱ ${escapeHtml(item.duration)}</span>`:''}${weatherMini(hourly)}</div><div class="card-actions">${item.mapQuery?`<a class="btn btn-secondary" href="${mapUrl(item.mapQuery)}" target="_blank" rel="noopener">Navigate</a>`:''}<button class="btn btn-ghost detail-toggle" aria-expanded="false">More info</button>${item.custom?`<button class="btn btn-danger-outline delete-stop" data-delete-stop="${item.id}">Delete</button>`:''}</div><div class="card-details" hidden>${detailRows(item)}</div></div>${transitConnector(item)}</article>`;}

  function editDoneTime(id){const current=S.completedTime(id)||tripTime24();openDialog('Edit completed time',`<label>Completed time<input id="done-time-input" type="time" value="${current}"></label><p class="muted dialog-help">This is shown in NYC / Toronto time so you can compare actual progress against the plan.</p><button id="save-done-time" class="btn btn-primary btn-block">Save time</button>`,()=>{$('#save-done-time').addEventListener('click',()=>{const v=$('#done-time-input').value;if(v)S.setCompletedTime(id,v);closeDialog();page==='itinerary'?renderItinerary():renderHome();});});}
  function bindSharedInteractions(root=document){
    $$('.status-toggle',root).forEach(btn=>btn.addEventListener('click',()=>{const id=btn.dataset.statusId,done=S.status(id)==='done';S.setStatus(id,done?'pending':'done',done?null:tripTime24());page==='itinerary'?renderItinerary():renderHome();}));
    $$('[data-edit-time]',root).forEach(btn=>btn.addEventListener('click',()=>editDoneTime(btn.dataset.editTime)));
    $$('.detail-toggle',root).forEach(btn=>btn.addEventListener('click',()=>{const details=btn.closest('.trip-card').querySelector('.card-details'),open=details.hidden;details.hidden=!open;btn.setAttribute('aria-expanded',String(open));btn.textContent=open?'Hide info':'More info';}));
    $$('.note-field',root).forEach(area=>{let timer;area.addEventListener('input',()=>{clearTimeout(timer);timer=setTimeout(()=>S.setNote(area.dataset.noteId,area.value),250);});area.addEventListener('blur',()=>S.setNote(area.dataset.noteId,area.value));});
  }

  function clockParts(zone){const f=new Intl.DateTimeFormat('en-US',{timeZone:zone,weekday:'short',month:'short',day:'numeric',hour:'numeric',minute:'2-digit',timeZoneName:'short'});return f.format(new Date());}
  function startWorldClocks(){const update=()=>{const a=$('#clock-nyc'),b=$('#clock-india');if(a)a.textContent=clockParts(D.trip.timezone);if(b)b.textContent=clockParts(D.trip.indiaTimezone||'Asia/Kolkata');};update();setInterval(update,30000);}
  async function renderHome(){const date=tripDayForNow(),day=D.days.find(d=>d.key===date)||D.days[0],p=progress(date),next=nextStop(date);let weather=null,weatherMeta='';try{const res=await TripWeather.fetchForecast();weather=TripWeather.dailyFor(res.data,date);weatherMeta=minutesLabel(res.ts)+(res.cached?' · cached':'');}catch{}
    $('#app').innerHTML=`<section class="hero"><div class="hero-skyline" aria-hidden="true"></div><div class="hero-content"><p class="eyebrow">Private city trip</p><h1>NYC 2026</h1><p>Toronto → New York City</p><strong>September 12–15, 2026</strong><div class="countdown-pill">${countdownText()}</div></div></section><section class="world-time-strip" aria-label="Current times"><div><span>🗽 ${escapeHtml(D.trip.homeTimeLabel||'NYC / Toronto')}</span><b id="clock-nyc">—</b></div><div><span>🇮🇳 India</span><b id="clock-india">—</b></div></section><a class="birthday-callout" href="itinerary.html?day=2026-09-15"><span>🎂</span><div><small>Tuesday · September 15</small><b>Baby’s Birthday in NYC</b><p>MoMA, birthday lunch, Fifth Avenue & Hudson Yards →</p></div></a><section class="dashboard-grid"><article class="dash-card accent-card"><span class="dash-kicker">Next up</span>${next?`<div class="next-icon">${typeIcon(next.type)}</div><h2>${escapeHtml(next.title)}</h2><p>${fmtTime(next.time)} · ${escapeHtml(day.short)}</p><a href="itinerary.html?day=${date}#${next.id}" class="text-link">Open itinerary →</a>`:'<h2>All done for this day</h2>'}</article><article class="dash-card weather-dash-card"><span class="dash-kicker">Weather</span><div class="weather-card-body">${weather?`<div class="weather-big"><span>${weather.icon}</span><b>${weather.max}°</b></div><h3>${weather.label}</h3><p>${weather.min}° low · Rain ${weather.rain??'—'}%</p><small>${weatherMeta}</small>`:'<h2>Forecast coming closer to the trip</h2><p>Live NYC weather appears automatically when the dates are within the forecast window.</p>'}</div><a href="weather.html" class="text-link weather-full-link">Full forecast →</a></article><article class="dash-card"><span class="dash-kicker">Today's progress</span><div class="progress-number">${p.done}<small> / ${p.total}</small></div><div class="progress-track"><span style="width:${p.pct}%"></span></div><p>${p.pct}% complete</p></article></section><section class="section-head"><div><p class="eyebrow">${day.label}</p><h2>Today's plan</h2></div><a href="itinerary.html?day=${date}" class="btn btn-secondary">Open full day</a></section><div class="compact-plan">${allStopsForDay(date).filter(x=>!x.routeMarker).slice(0,8).map(x=>{const r=S.statusRecord(x.id);return`<a href="itinerary.html?day=${date}#${x.id}" class="compact-row ${r.status==='done'?'done':''}"><span>${r.status==='done'?'✓':'○'}</span><time>${fmtTime(x.time)}</time><b>${escapeHtml(x.title)}${r.status==='done'&&r.completedTime?` <small class="compact-done-time">· done ${humanTime(r.completedTime)}</small>`:''}</b></a>`;}).join('')}</div><section class="quick-grid"><a class="quick-tile" href="itinerary.html"><span>◷</span><b>Itinerary</b><small>Five date tabs</small></a><a class="quick-tile" href="food.html"><span>♨</span><b>Food</b><small>Meal-by-meal choices</small></a><a class="quick-tile" href="drive.html"><span>➜</span><b>Drive</b><small>Toronto ↔ NYC</small></a><a class="quick-tile" href="weather.html"><span>☀</span><b>Weather</b><small>NYC forecast</small></a></section>`;
    startWorldClocks();
  }

  let activeDay=new URLSearchParams(location.search).get('day')||S.get('active_day',tripDayForNow());if(!D.days.some(d=>d.key===activeDay))activeDay=D.days[0].key;
  async function renderItinerary(){S.set('active_day',activeDay);let weatherData=null;try{weatherData=(await TripWeather.fetchForecast()).data;}catch{}const day=D.days.find(d=>d.key===activeDay),p=progress(activeDay);$('#app').innerHTML=`<section class="page-hero compact"><p class="eyebrow">Master itinerary</p><h1>${escapeHtml(day.title)}</h1><p>${escapeHtml(day.label)}</p>${day.special?`<div class="special-day-chip">🎂 ${escapeHtml(day.special)}</div>`:''}</section><div class="day-tabs" role="tablist">${D.days.map(d=>`<button class="day-tab ${d.key===activeDay?'active':''}" data-day="${d.key}">${d.short}</button>`).join('')}</div>${['2026-09-13','2026-09-14','2026-09-15'].includes(activeDay)?`<details class="transit-help"><summary>🚇 First time on the NYC subway? Quick guide</summary><div><p><b>Read the direction before the line:</b> Uptown usually means north; Downtown usually means south. A route letter/number alone is not enough because the same line runs both ways.</p><p><b>Pay with OMNY:</b> tap a contactless card, phone or watch at the turnstile/bus reader. One payment method can pay for up to four riders, but only the first rider’s taps count toward that payment method’s weekly fare cap.</p><p><b>For this trip:</b> expand any subway/bus connector between itinerary cards to see the exact station, line, direction, transfer and exit/walk instructions.</p><div class="chip-row"><a class="chip-link" href="https://www.mta.info/map/5256" target="_blank" rel="noopener">MTA Subway Map ↗</a><a class="chip-link" href="https://www.mta.info/planned-service-changes" target="_blank" rel="noopener">Service Changes ↗</a></div></div></details>`:''}<section class="day-summary"><div><b>${p.done} of ${p.total}</b><span> activities complete</span></div><div class="progress-track"><span style="width:${p.pct}%"></span></div><button class="btn btn-secondary" id="add-stop">+ Add stop</button></section><section class="timeline">${allStopsForDay(activeDay).map(item=>renderTimelineCard(item,weatherData,activeDay)).join('')}</section>`;$$('.day-tab').forEach(b=>b.addEventListener('click',()=>{activeDay=b.dataset.day;history.replaceState(null,'',`?day=${activeDay}`);renderItinerary();}));$('#add-stop').addEventListener('click',()=>openStopDialog(activeDay));$$('.delete-stop').forEach(b=>b.addEventListener('click',()=>{if(confirm('Delete this locally added stop?')){S.setCustomStops(S.customStops().filter(x=>x.id!==b.dataset.deleteStop));renderItinerary();}}));bindSharedInteractions();if(location.hash){const target=document.getElementById(location.hash.slice(1));if(target)setTimeout(()=>target.scrollIntoView({behavior:'smooth',block:'center'}),100);}}
  function openStopDialog(day){openDialog('Add itinerary stop',`<label>Type<select id="new-stop-type"><option value="place">Place / activity</option><option value="food">Food</option><option value="drive">Driving stop</option></select></label><label>Time<input id="new-stop-time" placeholder="e.g. 14:30 or TBD"></label><label>Name<input id="new-stop-title" required placeholder="Stop name"></label><label>Subtitle<input id="new-stop-subtitle" placeholder="Optional"></label><label>Map search<input id="new-stop-map" placeholder="Place or address"></label><button id="save-stop" class="btn btn-primary btn-block">Add stop</button>`,()=>{$('#save-stop').addEventListener('click',()=>{const title=$('#new-stop-title').value.trim();if(!title)return;const arr=S.customStops();arr.push({id:`custom-${Date.now()}`,day,time:$('#new-stop-time').value.trim()||'TBD',type:$('#new-stop-type').value,title,subtitle:$('#new-stop-subtitle').value.trim(),duration:'TBD',travelToNext:'—',travelMode:'walk',ticketStatus:'none',mapQuery:$('#new-stop-map').value.trim(),details:{about:'Added locally on this device.'}});S.setCustomStops(arr);closeDialog();renderItinerary();});});}

  function allRestaurants(){return[...D.curatedRestaurants,...S.customRestaurants()];}function mealOptions(meal){return allRestaurants().filter(r=>!r.mealIds||r.mealIds.includes(meal.id));}
  function renderFood(){const groups=D.days.slice(0,4).map(day=>{const meals=D.meals.filter(m=>m.day===day.key);if(!meals.length)return'';return`<section class="food-day"><div class="section-head"><div><p class="eyebrow">${day.short}</p><h2>${escapeHtml(day.title)}</h2></div></div>${meals.map(renderMeal).join('')}</section>`;}).join('');const saved=(D.savedFoodLinks||[]).map((x,i)=>`<a class="link-card" href="${x.url}" target="_blank" rel="noopener"><span>↗</span><b>${escapeHtml(x.label||`Saved food place ${i+1}`)}</b><small>Your saved Maps link</small></a>`).join('');$('#app').innerHTML=`<section class="page-hero compact"><p class="eyebrow">Itinerary-wise dining</p><h1>Food</h1><p>Select a restaurant for each meal. Menu links are included where available. Choices stay local to this device.</p></section>${groups}${saved?`<section class="link-group saved-food-links"><h2>Your saved food links</h2><div class="link-grid">${saved}</div></section>`:''}<section class="section-head"><div><p class="eyebrow">Local additions</p><h2>Your restaurants</h2></div><button id="add-restaurant" class="btn btn-primary">+ Add restaurant</button></section><div id="custom-restaurants">${renderCustomRestaurantList()}</div>`;$('#add-restaurant').addEventListener('click',openRestaurantDialog);bindFood();if(location.hash){const target=document.getElementById(location.hash.slice(1));if(target)setTimeout(()=>target.scrollIntoView({behavior:'smooth',block:'center'}),100);}}
  function renderMeal(meal){if(meal.fixed)return`<article class="meal-card" id="${meal.id}"><div><span class="badge badge-booked">${meal.fixedName==='Bungalow'?'✓ Booked':'Fixed'}</span><h3>${escapeHtml(meal.label)}</h3><p>${escapeHtml(meal.area)}</p></div><div class="selected-meal"><strong>${escapeHtml(meal.fixedName)}</strong><div class="option-links">${meal.fixedUrl?`<a href="${meal.fixedUrl}" target="_blank" rel="noopener">Website</a>`:''}${meal.fixedMenuUrl?`<a href="${meal.fixedMenuUrl}" target="_blank" rel="noopener">Menu</a>`:''}</div>${meal.recommendations?.length?`<details class="meal-recs"><summary>Our non-seafood recommendations</summary><ul>${meal.recommendations.map(x=>`<li>${escapeHtml(x)}</li>`).join('')}</ul></details>`:''}</div></article>`;const options=mealOptions(meal),selected=S.selectedRestaurant(meal.id);return`<article class="meal-card" id="${meal.id}"><div class="meal-head"><div><span class="badge">${escapeHtml(meal.area)}</span><h3>${escapeHtml(meal.label)}</h3></div><span class="selection-state">${selected?'Selected':'Not selected'}</span></div><div class="restaurant-options">${options.length?options.map(r=>`<label class="restaurant-option ${selected===r.id?'selected':''}"><input type="radio" name="${meal.id}" value="${r.id}" ${selected===r.id?'checked':''}><span><b>${escapeHtml(r.name)}</b><small>${escapeHtml(r.cuisine||'')} ${r.area?'· '+escapeHtml(r.area):''}</small>${r.note?`<em>${escapeHtml(r.note)}</em>`:''}</span><span class="option-links">${r.url?`<a href="${r.url}" target="_blank" rel="noopener">Site</a>`:''}${r.menuUrl?`<a href="${r.menuUrl}" target="_blank" rel="noopener">Menu</a>`:''}${r.mapQuery?`<a href="${mapUrl(r.mapQuery)}" target="_blank" rel="noopener">Maps</a>`:''}</span></label>`).join(''):'<div class="empty-state"><b>No curated options yet.</b><p>You can add one locally now.</p></div>'}</div>${selected?`<button class="text-button clear-meal" data-meal="${meal.id}">Clear selection</button>`:''}</article>`;}
  function renderCustomRestaurantList(){const arr=S.customRestaurants();if(!arr.length)return'<div class="empty-state wide"><b>No local restaurants added.</b><p>Use Add restaurant if you discover something during the trip.</p></div>';return`<div class="restaurant-library">${arr.map(r=>`<article><div><b>${escapeHtml(r.name)}</b><small>${escapeHtml(r.cuisine||'')} ${r.area?'· '+escapeHtml(r.area):''}</small></div><button class="text-button delete-restaurant" data-id="${r.id}">Delete</button></article>`).join('')}</div>`;}
  function bindFood(){$$('input[type=radio][name^="sep"]').forEach(r=>r.addEventListener('change',()=>{S.setSelectedRestaurant(r.name,r.value);renderFood();}));$$('.clear-meal').forEach(b=>b.addEventListener('click',()=>{S.setSelectedRestaurant(b.dataset.meal,null);renderFood();}));$$('.delete-restaurant').forEach(b=>b.addEventListener('click',()=>{S.setCustomRestaurants(S.customRestaurants().filter(r=>r.id!==b.dataset.id));renderFood();}));}
  function openRestaurantDialog(){const mealChecks=D.meals.filter(m=>!m.fixed).map(m=>`<label class="check-row"><input type="checkbox" name="meal-assign" value="${m.id}"><span>${D.days.find(d=>d.key===m.day)?.short} · ${escapeHtml(m.label)}</span></label>`).join('');openDialog('Add restaurant',`<label>Name<input id="rest-name" required></label><label>Cuisine<input id="rest-cuisine" placeholder="e.g. Italian"></label><label>Area<input id="rest-area" placeholder="e.g. Financial District"></label><label>Website<input id="rest-url" type="url" placeholder="https://"></label><label>Menu link<input id="rest-menu" type="url" placeholder="https://"></label><label>Google Maps search<input id="rest-map" placeholder="Restaurant name + NYC"></label><fieldset><legend>Show for these meals</legend>${mealChecks}</fieldset><button id="save-rest" class="btn btn-primary btn-block">Save restaurant</button>`,()=>{$('#save-rest').addEventListener('click',()=>{const name=$('#rest-name').value.trim();if(!name)return;const mealIds=$$('input[name="meal-assign"]:checked').map(x=>x.value),arr=S.customRestaurants();arr.push({id:`rest-${Date.now()}`,name,cuisine:$('#rest-cuisine').value.trim(),area:$('#rest-area').value.trim(),url:$('#rest-url').value.trim(),menuUrl:$('#rest-menu').value.trim(),mapQuery:$('#rest-map').value.trim(),mealIds});S.setCustomRestaurants(arr);closeDialog();renderFood();});});}

  function renderDrive(){$('#app').innerHTML=`<section class="page-hero compact"><p class="eyebrow">Road trip</p><h1>Drive</h1><p>Toronto ↔ NYC route, planned breaks, parking and the overnight return. Check live traffic before each long leg.</p></section><div class="seg-tabs"><button class="seg active" data-drive="toNYC">To NYC</button><button class="seg" data-drive="parking">Parking</button><button class="seg" data-drive="return">Return</button></div><div id="drive-content"></div>`;const draw=key=>{$$('.seg').forEach(x=>x.classList.toggle('active',x.dataset.drive===key));const data=D.drive[key];const cards=data.map((x,i)=>`<article class="route-card"><span class="route-num">${i+1}</span><div class="route-card-copy"><div class="route-card-head">${x.time?`<time>${escapeHtml(x.time)}</time>`:''}${x.meta?`<span>${escapeHtml(x.meta)}</span>`:''}</div><h3>${escapeHtml(x.title)}</h3><p>${escapeHtml(x.note)}</p>${x.url?`<a class="text-link" href="${x.url}" target="_blank" rel="noopener">Open link →</a>`:''}</div></article>`).join('');let actions='';if(key==='toNYC')actions=`<a class="btn btn-primary map-wide" href="${D.driveLinks.toNYC}" target="_blank" rel="noopener">Full Route · Toronto → NYC</a>`;if(key==='return')actions=`<a class="btn btn-primary map-wide" href="${D.driveLinks.return}" target="_blank" rel="noopener">Full Route · NYC → Toronto via Niagara</a><p class="route-note">Use live Google Maps at departure; traffic or border waits can change the fastest route.</p>`;if(key==='parking')actions=`<a class="btn btn-primary map-wide" href="${D.driveLinks.spotHero}" target="_blank" rel="noopener">Open SpotHero NYC Parking</a>`;$('#drive-content').innerHTML=`<div class="route-stack">${cards}</div>${actions}`;};$$('.seg').forEach(b=>b.addEventListener('click',()=>draw(b.dataset.drive)));draw('toNYC');}

  async function renderWeather(){$('#app').innerHTML='<section class="page-hero compact"><p class="eyebrow">Live + cached</p><h1>NYC Weather</h1><p>Forecast data is fetched from Open-Meteo and cached locally for offline viewing.</p></section><div id="weather-content" class="weather-page"><div class="loading-card">Loading forecast…</div></div>';try{const res=await TripWeather.fetchForecast();const cards=D.days.slice(0,4).map(day=>{const w=TripWeather.dailyFor(res.data,day.key);if(!w)return`<article class="forecast-card"><span>${day.short}</span><h3>Forecast not available yet</h3><p>It will appear automatically when the trip falls inside the provider's forecast window.</p></article>`;const hours=['09:00','12:00','15:00','18:00','21:00'].map(t=>{const h=TripWeather.hourlyFor(res.data,day.key,t);return h?`<div><time>${t}</time><span>${h.icon}</span><b>${h.temp}°</b><small>${h.rain}% rain</small></div>`:''}).join('');return`<article class="forecast-card"><div class="forecast-main"><div><span>${day.short}</span><h3>${w.label}</h3><p>${w.min}° low · Rain ${w.rain}%</p></div><div class="weather-big"><span>${w.icon}</span><b>${w.max}°</b></div></div><div class="hour-strip">${hours}</div></article>`;}).join('');$('#weather-content').innerHTML=`<div class="weather-meta"><span>${res.cached?'Cached forecast':'Live forecast'}</span><small>${minutesLabel(res.ts)}</small><button id="refresh-weather" class="btn btn-secondary">Refresh</button></div>${cards}`;$('#refresh-weather').addEventListener('click',async()=>{await TripWeather.fetchForecast(true);renderWeather();});}catch{$('#weather-content').innerHTML='<div class="empty-state wide"><b>Weather unavailable.</b><p>No live connection and no cached forecast exists on this device yet.</p><button class="btn btn-secondary" onclick="location.reload()">Try again</button></div>';}}
  function renderLinks(){const custom=S.get('custom_links',[]),all=[...D.links,...custom],cats=[...new Set(all.map(x=>x.category))];$('#app').innerHTML=`<section class="page-hero compact"><p class="eyebrow">Quick access</p><h1>Important Links</h1><p>Reusable trip resources. Attraction-specific links also live inside itinerary cards.</p></section>${cats.map(cat=>`<section class="link-group"><h2>${escapeHtml(cat)}</h2><div class="link-grid">${all.filter(x=>x.category===cat).map(x=>`<a class="link-card" href="${x.url}" target="_blank" rel="noopener"><span>↗</span><b>${escapeHtml(x.title)}</b><small>${escapeHtml(cat)}</small></a>`).join('')}</div></section>`).join('')}<button id="add-link" class="fab">+ Add link</button>`;$('#add-link').addEventListener('click',()=>openDialog('Add link','<label>Title<input id="link-title"></label><label>URL<input id="link-url" type="url" placeholder="https://"></label><label>Category<input id="link-cat" value="Other"></label><button id="save-link" class="btn btn-primary btn-block">Save link</button>',()=>{$('#save-link').addEventListener('click',()=>{const title=$('#link-title').value.trim(),url=$('#link-url').value.trim();if(!title||!url)return;const arr=S.get('custom_links',[]);arr.push({title,url,category:$('#link-cat').value.trim()||'Other'});S.set('custom_links',arr);closeDialog();renderLinks();});}));}
  function renderLists(){const state=S.listState();$('#app').innerHTML=`<section class="page-hero compact"><p class="eyebrow">Offline checklist</p><h1>${escapeHtml(state.name)}</h1><p>Everything on this page is stored only on this device.</p></section><section class="list-toolbar"><button id="rename-list" class="btn btn-secondary">Rename</button><button id="clear-checked" class="btn btn-ghost">Clear checked</button></section><form id="add-list-item" class="add-row"><input id="list-new" placeholder="Add an item…" autocomplete="off"><button class="btn btn-primary">Add</button></form><div class="checklist">${state.items.length?state.items.map((x,i)=>`<label class="checklist-row ${x.done?'done':''}"><input type="checkbox" data-list-index="${i}" ${x.done?'checked':''}><span>${escapeHtml(x.text)}</span><button type="button" class="delete-list" data-delete-list="${i}">×</button></label>`).join(''):'<div class="empty-state wide"><b>Your checklist is empty.</b><p>Add packing, car, shopping or pre-trip items above.</p></div>'}</div>`;$('#add-list-item').addEventListener('submit',e=>{e.preventDefault();const v=$('#list-new').value.trim();if(!v)return;const st=S.listState();st.items.push({text:v,done:false});S.setListState(st);renderLists();});$$('[data-list-index]').forEach(x=>x.addEventListener('change',()=>{const st=S.listState();st.items[+x.dataset.listIndex].done=x.checked;S.setListState(st);renderLists();}));$$('[data-delete-list]').forEach(x=>x.addEventListener('click',()=>{const st=S.listState();st.items.splice(+x.dataset.deleteList,1);S.setListState(st);renderLists();}));$('#clear-checked').addEventListener('click',()=>{const st=S.listState();st.items=st.items.filter(x=>!x.done);S.setListState(st);renderLists();});$('#rename-list').addEventListener('click',()=>{const name=prompt('List name',S.listState().name);if(name?.trim()){const st=S.listState();st.name=name.trim();S.setListState(st);renderLists();}});}
  function renderMore(){$('#app').innerHTML='<section class="page-hero compact"><p class="eyebrow">More</p><h1>Trip tools</h1><p>Weather, useful links, lists and local-device settings.</p></section><div class="more-grid"><a class="more-card" href="weather.html"><span>☀</span><div><b>Weather</b><small>Live + cached NYC forecast</small></div><i>›</i></a><a class="more-card" href="links.html"><span>↗</span><div><b>Links</b><small>Attractions, transport and maps</small></div><i>›</i></a><a class="more-card" href="lists.html"><span>✓</span><div><b>Lists</b><small>Your offline trip checklist</small></div><i>›</i></a><a class="more-card" href="settings.html"><span>⚙</span><div><b>Settings</b><small>Local data and device access</small></div><i>›</i></a></div>';}
  function renderSettings(){$('#app').innerHTML='<section class="page-hero compact"><p class="eyebrow">This device</p><h1>Settings</h1><p>Authentication and dynamic trip data are stored locally in this browser.</p></section><div class="settings-stack"><article class="settings-card"><div><h3>Forget this device</h3><p>Locks the app and requires the trip password next time.</p></div><button id="forget-device" class="btn btn-danger-outline">Forget device</button></article><article class="settings-card"><div><h3>Export local changes</h3><p>Downloads notes, selections, checklist items and completion times as JSON.</p></div><button id="export-data" class="btn btn-secondary">Export</button></article><article class="settings-card"><div><h3>Import local changes</h3><p>Import a JSON export from another device if you want to copy local state manually.</p></div><label class="btn btn-secondary file-btn">Import<input id="import-data" type="file" accept="application/json" hidden></label></article><article class="settings-card danger-zone"><div><h3>Reset dynamic data</h3><p>Clears notes, statuses/times, restaurant selections, custom items, lists and weather cache. The device stays unlocked.</p></div><button id="reset-data" class="btn btn-danger-outline">Reset data</button></article><article class="settings-card"><div><h3>Static-site password</h3><p>The password is unchanged for this update. We can replace it separately before your final publish.</p></div><span class="badge">V3</span></article></div>';$('#forget-device').addEventListener('click',()=>{if(confirm('Forget this device and lock the app?'))TripAuth.forget();});$('#reset-data').addEventListener('click',()=>{if(confirm('Clear all dynamic data stored on this device?')){S.resetDynamic();location.reload();}});$('#export-data').addEventListener('click',()=>{const blob=new Blob([JSON.stringify(S.export(),null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='nyc-2026-local-data.json';a.click();URL.revokeObjectURL(a.href);});$('#import-data').addEventListener('change',async e=>{const f=e.target.files[0];if(!f)return;try{S.import(JSON.parse(await f.text()));alert('Local data imported.');location.reload();}catch{alert('Could not import this file.');}});}
  function openDialog(title,body,after){const d=document.createElement('dialog');d.id='app-dialog';d.innerHTML=`<div class="dialog-head"><h2>${title}</h2><button class="dialog-close" aria-label="Close">×</button></div><div class="dialog-body">${body}</div>`;document.body.appendChild(d);d.querySelector('.dialog-close').addEventListener('click',closeDialog);d.addEventListener('click',e=>{if(e.target===d)closeDialog();});d.showModal();after?.();}
  function closeDialog(){const d=$('#app-dialog');if(d){d.close();d.remove();}}window.closeDialog=closeDialog;
  function registerSW(){if('serviceWorker'in navigator)navigator.serviceWorker.register('./sw.js').catch(()=>{});}
  document.addEventListener('DOMContentLoaded',()=>{nav();setTopTheme();matchMedia('(prefers-color-scheme: dark)').addEventListener?.('change',setTopTheme);registerSW();({home:renderHome,itinerary:renderItinerary,food:renderFood,drive:renderDrive,weather:renderWeather,links:renderLinks,lists:renderLists,more:renderMore,settings:renderSettings}[page]||renderHome)();});
})();
